import { RecupvaleurDirective } from './recupvaleur.directive';

describe('RecupvaleurDirective', () => {
  it('should create an instance', () => {
    const directive = new RecupvaleurDirective();
    expect(directive).toBeTruthy();
  });
});
